package com.sc.study.jiuzhang.dtgh;/** * @author yingqi * @date 2021/12/10 */public class CanJump {    public static void main(String[] args) {        int[] brand = new int[]{2, 2, 0, 1};        int[] brand2 = new int[]{4, 3, 2, 1, 0, 1};        System.out.println(checkCanJump(brand));        System.out.println(checkCanJump(brand2));    }    private static boolean checkCanJump(int[] origin) {        if (null == origin) {            return false;        }        boolean[] dp = new boolean[origin.length];        //注意初始值        dp[0] = true;        for (int i = 1; i < origin.length; i++) {            for (int j = 0; j < i; j++) {                //这个逻辑的意思是只要指定要跳位置的前面任意个位置可以跳到就为true                if (dp[j] && origin[j] + j >= i) {                    dp[i] = true;                    break;                }            }        }        return dp[origin.length - 1];    }}